# Swarm Monitoring

Swarm Monitoring.

**Grafana** username: `admin` and password: `example`.

## Usage

- Deploy stack

    `./run stack deploy`

- Remove stack

    `./run stack rm`

- Remove and deploy stack

    `./run stack redeploy`
