# Envoy Docker Swarm Stack

Example of Envoy load balancer and proxy.

## Usage

- Deploy stack

    `./run stack deploy`

- Remove stack

    `./run stack rm`

- Remove and deploy stack

    `./run stack redeploy`

