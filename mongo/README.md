# MongoDB Docker Swarm Stack

MongoDB Docker Swarm stack with Mongo Express web clients.

## Usage

- Deploy stack

    `./run stack deploy`

- Remove stack

    `./run stack rm`

- Remove and deploy stack

    `./run stack redeploy`
